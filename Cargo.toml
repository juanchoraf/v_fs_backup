[package]
name = "v_fs_backup"
version = "0.1.11"
edition = "2024"

[dependencies]
dialoguer = "0.12.0"
rustyline = "18.0.1"
v_concat = "0.1.3"
zstd = { version = "0.14.0", default-features = false }

[target.'cfg(unix)'.dependencies]
libc = "0.2.189"

[profile.dev]
incremental = false

[profile.test]
incremental = false

[profile.release]
lto = false
codegen-units = 16
incremental = false
strip = "none"
